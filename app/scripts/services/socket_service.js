'use strict';

var myapp = angular.module("phApp.socketservice", ['ngStorage','ngResource']);

myapp.factory('socket', function() {
  /*var socket = io.connect('http://localhost:9005');
  socket.on('connect',function() {
    console.log("socket connected");
  });
  return socket;*/
});
